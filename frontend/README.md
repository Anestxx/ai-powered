# Codyssey frontend

This is the current React frontend for Codyssey.

```bash
npm install
npm start
```

The `legacy-codyssey/` and `legacy-root-flat/` directories retain earlier frontend copies for reference; they are not part of the active app.
